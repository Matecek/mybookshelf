{% extends 'base.html.twig' %}

{% block title %}Moje książki (demo){% endblock %}

{% block body %}
    <div class="max-w-5xl mx-auto mt-10 px-4">
        <h1 class="text-2xl font-bold mb-6">Książki w bazie</h1>

        {% if books is empty %}
            <div class="bg-blue-100 text-blue-800 p-4 rounded">Brak danych w bazie.</div>
        {% else %}
            <div class="overflow-x-auto">
                <table class="min-w-full bg-white shadow rounded divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tytuł</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Autor</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ocena</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Dodano</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Właściciel (email)</th>
                    </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                    {% for b in books %}
                        <tr onclick="window.location='{{ path('book_show', {id: b.id}) }}'"
                            class="cursor-pointer hover:bg-gray-50 transition-colors">
                            <td class="px-6 py-4">{{ b.title }}</td>
                            <td class="px-6 py-4">{{ b.author }}</td>
                            <td class="px-6 py-4">
                                {% if b.status == 'planned' %}planowana
                                {% elseif b.status == 'reading' %}czytana
                                {% else %}przeczytana{% endif %}
                            </td>
                            <td class="px-6 py-4">{{ b.rating is not null ? b.rating ~ '/10' : '—' }}</td>
                            <td class="px-6 py-4">{{ b.createdAt|date('Y-m-d H:i') }}</td>
                            <td class="px-6 py-4">{{ b.user ? b.user.email : '—' }}</td>
                        </tr>
                    {% endfor %}

                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}